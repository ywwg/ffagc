<%= link_to "Register as an artist", :controller => "artists", :action => "signup" %> to access submit grant applications.

<h2>Have an account?</h2>

<div class="row">
  <div class="col-md-4">
    <%= form_tag(controller: 'sessions', action:'create_artist') do %>
      <div class="form-group">
        <%= label :session, :email %>
        <%= text_field :session, :email, :placeholder => "Email Address", class: 'form-control' %>
      </div>

      <div class="form-group">
        <%= label :session, :password %>
        <%= password_field :session, :password, :placeholder => "Password", class: 'form-control' %>
      </div>

      <div class="form-group">
        <%= submit_tag("Sign in", class: "btn btn-primary") %>
        <%= link_to "Forgot password?", new_password_reset_path(type: "artists") %>
      </div>
    <% end %>
  </div>
</div>
